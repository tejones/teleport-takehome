kind: role
version: v7
metadata:
  name: app-deployer
spec:
  allow:
    kubernetes_groups:
      - teleport-app-deployers
    kubernetes_labels:
      "*": "*"
    kubernetes_resources:
      - kind: "*"
        namespace: nginx-app
        name: "*"
        verbs:
          - "*"
