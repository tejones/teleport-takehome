apiVersion: v1
kind: Service
metadata:
  name: peach-cheesecake
  namespace: nginx-app
spec:
  selector:
    app: peach-cheesecake
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
